# Implementation Specs

Generated by pb-improve on 2026-06-27. Execute via `/pb-build codebase-quality`.

## Execution order & status

| Spec | Findings | Priority | Effort | Status |
| --- | --- | --- | --- | --- |
| 2026-06-27-01-codebase-quality | 22 findings (1-22) | P1 | M | TODO |

## Finding details

| # | Finding | Category | Effort | Tasks |
|---|---------|----------|--------|-------|
| 1 | gRPC recursive message stack overflow | bug | S | Task 1.1 |
| 2 | SSRF via unvalidated upstream URL | security | S | Tasks 2.1, 2.2 |
| 3 | SSRF via callback URL | security | S | Task 3.1 |
| 4 | Faker enum always picks first | bug | S | Task 4.1 |
| 5 | Faker caps minItems/minLength | bug | S | Task 5.1 |
| 6 | Integer faker overflow | bug | S | Task 6.1 |
| 7 | Content-type fallback wrong schema | bug | S | Task 7.1 |
| 8 | Prefer:code silent fallback | bug | S | Task 8.1 |
| 9 | Proxy forwards auth headers | security | S | Task 9.1 |
| 10 | Error responses leak file paths | security | S | Task 10.1 |
| 11 | Callback URL non-body tokens | bug | S | Task 11.1 |
| 12 | named_examples non-deterministic | bug | S | Task 12.1 |
| 13 | Triplicated hash function | tech debt | S | Task 13.1 |
| 14 | Duplicated JSON Pointer resolver | tech debt | S | Task 14.1 |
| 15 | Dead code enums | tech debt | S | Task 15.1 |
| 16 | ResolvedDocument pointless newtype | tech debt | S | Task 16.1 |
| 17 | ws_url() hardcodes /ws | bug | S | Task 17.1 |
| 18 | normalize_schema clones tree | performance | M | Task 18.1 |
| 19 | No proxy integration tests | tests | M | Task 19.1 |
| 20 | AGENTS.md drift | dx | M | Task 20.1 |
| 21 | Validator cache key serialization | performance | S | Task 21.1 |
| 22 | No BDD feature files | tests | L | Task 22.1 |

## Findings considered and rejected

- CORRECTNESS-01 (validator cache race): Rejected — `scc::HashMap::insert_sync` returns `Err` when key exists, and the fallback `read_sync` is correct. The "race" described is a non-issue because `scc` handles concurrent insertion atomically. The losing thread reads the winning thread's value.
- CORRECTNESS-02 (canonicalization failure): Rejected — `unwrap_or_else(|_| base_dir.clone())` is a reasonable fallback for non-existent base dirs; the path comparison still works for the common case.
- CORRECTNESS-09 (named_examples HashMap): Already covered by Finding 12.
- CORRECTNESS-10 (port TOCTOU): Accepted as LOW, not worth fixing — the race window is narrow and the error message is clear enough.
- CORRECTNESS-11 (callback timeout): Accepted as LOW, deferred — fire-and-forget is the documented design. Add timeout when callbacks become a reliability concern.
- CORRECTNESS-13 (parse_optional_json_body): Accepted as LOW, deferred — error message is misleading but behavior is approximately correct.
- CORRECTNESS-14 (WS binary messages): Accepted as LOW, deferred — binary WS frames are uncommon for this use case.
- CORRECTNESS-15 (port 0 collision): Accepted as LOW, deferred — OS behavior is acceptable.
- SEC-005 (localhost binding): Accepted as MED, deferred — document the risk, not a code fix.
- SEC-006 (path traversal): Already mitigated by canonicalize+starts_with.
- SEC-007 (serde_yml version): Accepted as MED, monitoring — pin exact version if needed.
- SEC-008 (jsonschema DoS): Accepted as MED, monitoring — add timeout if needed.
- SEC-010 (seed predictability): By design — documented.
- DEP-001 (version ranges): Cargo.lock handles this in practice.
- TEST-01 through TEST-09: Partially covered by Finding 19 (proxy tests) and Finding 22 (BDD). Remaining test gaps deferred.
- TECH-DEBT-06 (expect_used override): Accepted, deferred — scope to dev-dependencies when convenient.
- TECH-DEBT-08 (redundant proxy validation): Accepted, deferred — harmless redundancy.
- ARCH-01 (SDK pass-through): By design — API boundary is intentional.
- DIRECTION-01 through DIRECTION-04: Noted for future consideration.
