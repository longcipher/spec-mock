# Project Context

Generated by pb-improve on 2026-06-27. This file provides shared context for all specs in `specs/`.

## Project Overview

- **Language(s):** Rust (edition 2024)
- **Framework(s):** axum (HTTP/WS), tonic (gRPC), tokio (async runtime)
- **Package Manager:** cargo (workspace)
- **Architecture:** Rust workspace with 1 CLI binary (`bin/spec-mock`) and 3 library crates (`crates/specmock-core`, `crates/specmock-runtime`, `crates/specmock-sdk`)

## Build & Test Commands

| Purpose   | Command                                      | Expected on success |
|-----------|----------------------------------------------|---------------------|
| Install   | `cargo build --workspace`                    | exit 0              |
| Lint      | `cargo +nightly clippy --all -- -D warnings` | exit 0, no errors   |
| Format    | `cargo +nightly fmt --all -- --check`        | exit 0              |
| Tests     | `cargo test --all-features`                  | all pass            |
| Full CI   | `just ci`                                    | all pass            |

Additional tooling:

- `typos` — typo checker
- `rumdl check .` — markdown linter
- `taplo fmt --check` — TOML formatter
- `cargo machete` — unused dependency checker

## Code Conventions

- **Naming:** snake_case for functions/variables, PascalCase for types, SCREAMING_SNAKE for constants
- **Error Handling:** `thiserror` for library error types, `eyre` for application-level (CLI binary)
- **State Management:** `Arc<HttpRuntime>` shared via axum `State`, `scc` for concurrent collections
- **Import Style:** grouped by stdlib → third-party → local, with `use` statements at module top
- **Lint Policy:** workspace-level clippy pedantic with specific overrides (see `Cargo.toml` `[workspace.lints.clippy]`)
- **Panic Policy:** `unwrap_used = "deny"`, `expect_used = "deny"`, `panic = "deny"` — no panics in production code
- **Debug Macros:** `dbg_macro = "deny"` — use `tracing` instead
- **Print Policy:** `print_stdout = "deny"`, `print_stderr = "deny"` in library code (CLI overrides)

## Key Components

| Module | Purpose | Key Files |
|--------|---------|-----------|
| specmock-core | Core types, schema validation, $ref resolution, deterministic faker | `crates/specmock-core/src/{lib,error,contract,schema,validate,ref_resolver,faker}.rs` |
| specmock-runtime | HTTP (OpenAPI), WebSocket (AsyncAPI), gRPC (Protobuf) mock servers | `crates/specmock-runtime/src/{lib,http/{mod,openapi,router,negotiate,proxy,ws_handler},ws/{mod,asyncapi},grpc/{mod,protobuf}}.rs` |
| specmock-sdk | Rust SDK for embedding mock server in tests (in-process + process mode) | `crates/specmock-sdk/src/{lib,server}.rs` |
| spec-mock (bin) | CLI entrypoint (`spec-mock serve`) | `bin/spec-mock/src/main.rs` |

## Existing Test Patterns

- **Unit Tests:** colocated with implementation (`#[cfg(test)] mod tests`) — extensive in core and runtime
- **Integration Tests:** `crates/specmock-runtime/tests/{http_openapi,ws_asyncapi,grpc_protobuf,prism_comparison}.rs`
- **SDK Tests:** `crates/specmock-sdk/tests/{sdk_embed,sdk_process}.rs`
- **Test Harness:** `crates/specmock-runtime/tests/harness/{mod,fuzzer,comparator,prism,request}.rs`
- **BDD:** No `.feature` files exist — this is a gap
- **Total:** 145 passing tests, 11 ignored (integration tests requiring Prism)

## Git History Signal

- **Active Areas:** All code is actively developed (7 commits total, all recent)
- **Recent Commits:** Prism parity overhaul, dependency updates, AGENTS.md updates, context7.json
- **Churn:** Evenly spread across all crates (new project)

## Architectural Decisions (Existing)

- **Radix-tree router:** Custom `PathRouter` for O(depth) path matching instead of linear scan
- **Deterministic faker:** Seeded ChaCha8Rng for reproducible mock data generation
- **$ref resolver:** Supports local JSON pointer and file-relative refs with allowed-root sandboxing and LRU cache
- **RFC 7807 errors:** All error responses use `application/problem+json` format
- **Prefer header:** Supports `code=`, `example=`, `dynamic=true` directives for response selection
- **Fire-and-forget callbacks:** OpenAPI callbacks are spawned as detached tokio tasks
